<section>
    <h1>工作人員</h1>
    <% foreach $jobs as $job %>
    <section>
        <h1><% $job->name|escape %></h1>
        <ul>
            <% foreach $staffs as $staff %>
            <% if $staff->jobId === $job->id %>
            <li><% $staff->name|escape %></li>
            <% /if %>
            <% /foreach %>
        </ul>
    </section>
    <% /foreach %>
</section>